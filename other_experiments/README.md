# Other Experiments

This folder contains experimental components that are not part of the core stack.

## Subfolders

- `context-verification`: Security-focused context verification.
- `nix`: Nix-based build system.
- `llamastash`: Rust-based model proxy (experimental).

## Warning

AI agents should avoid getting caught up in these experiments. Focus on the core stack (`continue.dev` and `llama-swap`).
